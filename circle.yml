dependencies:
  override:
    - npm install
checkout:
  post:
    - git submodule init 
    - git submodule update
test:
  post:
    - debug=1 npm run pkg
deployment:
  staging:
    branch: master
    heroku:
      appname: aura-explorer